NEXT_PUBLIC_API_URL=http://localhost:8000

# Sign-in (leave empty locally to run open, without Google)
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Same value as the backend's BACKEND_JWT_SECRET
BACKEND_JWT_SECRET=
